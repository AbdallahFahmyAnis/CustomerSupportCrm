---
name: speckit-converge
description: Check the codebase against the active CRM spec and append remaining tasks. Use when the user says converge, verify the spec, or after implement.
---

# Converge

1. Read `spec.md` success criteria and walk the code/UI paths on `http://localhost:5000`.
2. If gaps remain, append tasks to `tasks.md` (do not silently recode unrelated areas).
3. If complete, set spec Status to `Implemented` and report what was verified.
